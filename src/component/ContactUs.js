import React, { Component } from 'react';

class ContactUs extends Component {
    render() {
        return (
            <div>
                Error! error in path
            </div>
            );
    }
}

export default ContactUs;
